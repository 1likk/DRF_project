# Notes Project - Django REST API

> **Проект системы управления заметками на Django REST Framework**

Полнофункциональное API для создания, чтения, обновления и удаления заметок с архитектурой "клиент-сервер".

## 🚀 Технологии

**Backend:**
- Django 5.2
- Django REST Framework
- PostgreSQL / SQLite (настраиваемо)
- Python-decouple (управление настройками)
- CORS Headers (для фронтенда)
- Django Filters (фильтрация данных)

**Инструменты разработки:**
- Logging (отладка и мониторинг)
- Django Admin (управление данными)
- Python-decouple (конфигурация)

## 📋 Возможности

- ✅ CRUD операции для заметок
- ✅ RESTful API endpoints
- ✅ Поддержка PostgreSQL и SQLite
- ✅ CORS-совместимость для фронтенда
- ✅ Валидация данных
- ✅ Логирование операций
- ✅ Админ-панель Django
- ✅ Гибкая конфигурация

## 🛠 API Endpoints

```
GET    /api/v1/notes/          # Список заметок
POST   /api/v1/notes/          # Создание заметки
GET    /api/v1/notes/{id}/     # Детали заметки
PUT    /api/v1/notes/{id}/     # Обновление заметки
DELETE /api/v1/notes/{id}/     # Удаление заметки
```

## ⚙️ Установка и запуск

### 1. Клонирование проекта
```bash
git clone <repo-url>
cd notes_project
```

### 2. Настройка Backend
```bash
cd backend

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env  # Отредактируйте файл .env

# Применение миграций
python manage.py migrate

# Создание суперпользователя (опционально)
python manage.py createsuperuser

# Запуск сервера
python manage.py runserver
```

### 3. Переменные окружения (.env)
```env
SECRET_KEY=your-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL (опционально)
POSTGRES_DB=notesdrf
POSTGRES_USER=notesdrf  
POSTGRES_PASSWORD=notesdrf
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
```

## 🔧 Проверка проекта

```bash
# Проверка настроек
python manage.py check

# Проверка базы данных
python manage.py showmigrations

# Создание суперпользователя
python manage.py createsuperuser
```

## 📁 Структура проекта

```
notes_project/
├── backend/
│   ├── apps/
│   │   └── notes/          # Приложение заметок
│   │       ├── models.py   # Модель Note
│   │       ├── serializers.py  # DRF сериализаторы
│   │       ├── views.py    # API представления
│   │       └── urls.py     # URL маршруты
│   ├── notes_project/      # Настройки проекта
│   │   ├── settings.py     # Конфигурация Django
│   │   └── urls.py         # Главные URL
│   ├── manage.py           # Django управление
│   └── requirements.txt    # Зависимости
├── frontend/               # Будущий фронтенд
└── README.md
```

## 🎯 Особенности архитектуры

- **Разделение ответственности**: отдельные сериализаторы для создания/обновления/чтения
- **Валидация**: комплексная проверка данных на уровне сериализаторов  
- **Конфигурация**: гибкие настройки через переменные окружения
- **Масштабируемость**: готовность к добавлению новых функций
- **Администрирование**: полнофункциональная админ-панель

## 📝 Пример использования API

```bash
# Создание заметки
curl -X POST http://localhost:8000/api/v1/notes/ \
  -H "Content-Type: application/json" \
  -d '{"title": "Моя заметка", "content": "Содержание заметки"}'

# Получение списка заметок  
curl http://localhost:8000/api/v1/notes/

# Обновление заметки
curl -X PUT http://localhost:8000/api/v1/notes/1/ \
  -H "Content-Type: application/json" \
  -d '{"title": "Обновленная заметка", "content": "Новое содержание"}'
```

## 🔍 Дальнейшее развитие

- [ ] Аутентификация и авторизация пользователей
- [ ] Категории и теги для заметок  
- [ ] Полнотекстовый поиск
- [ ] API документация (Swagger/OpenAPI)
- [ ] Пагинация и фильтрация
- [ ] Фронтенд на React/Vue.js
- [ ] Docker контейнеризация
- [ ] CI/CD pipeline

---

## 🎬 Демонстрация для работодателей

### Быстрый старт (для технического интервью)
```bash
# Клонирование и запуск
git clone [repo-url]
cd notes_project/backend
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver

# Тестирование API
curl http://127.0.0.1:8000/api/v1/notes/
python manage.py check  # Проверка конфигурации
```

### Что демонстрирует проект
- **Backend навыки**: Django, DRF, PostgreSQL, Python
- **API Design**: RESTful архитектура, правильные HTTP методы
- **Quality Assurance**: Валидация данных, обработка ошибок, логирование
- **DevOps готовность**: ENV конфигурация, migrations, deployment-ready
- **Документация**: README, Postman коллекция, комментарии в коде

## 🔗 Дополнительные материалы

- 📋 **Postman коллекция**: `Notes_API_Postman_Collection.json`
- 🎯 **Подготовка к собеседованию**: `INTERVIEW_PREP.md`
- 📝 **Для резюме**: `RESUME_DESCRIPTION.md`

---

**Автор:** Бирлик Жумадил  
**Назначение:** Pet-проект для стажировки / портфолио
